{
  "name": "stock-tracking-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "start": "bun server.ts",
    "dev": "bun --watch server.ts",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && bun build server.ts --target=bun --outfile=/tmp/stock-tracker-server-check.js && bun build public/app.ts --target=browser --outfile=/tmp/stock-tracker-app-check.js"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
