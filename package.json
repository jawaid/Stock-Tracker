{
  "name": "stock-tracking-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "start": "bun server.js",
    "dev": "bun --watch server.js",
    "check": "bun build server.js --target=bun --outfile=/tmp/stock-tracker-server-check.js && bun build public/app.js --target=browser --outfile=/tmp/stock-tracker-app-check.js"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
